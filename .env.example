# Documentation template only: .env files are NOT automatically loaded.
# Supply these variables in the environment of the terminal launching Tiled.
TILED_AI_PROVIDER=deterministic
# Required only when TILED_AI_PROVIDER=openai:
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
OPENAI_MODEL=YOUR_RESPONSES_STRUCTURED_OUTPUTS_MODEL
